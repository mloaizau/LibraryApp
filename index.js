/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { LibraryApp } from './src/LibraryApp';

AppRegistry.registerComponent(appName, () => LibraryApp);
